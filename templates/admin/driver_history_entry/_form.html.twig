{{ form_start(form) }}
    {{ form_row(form.driver) }}
    {{ form_row(form.text) }}
    {{ form_row(form.occurredAt) }}
    {{ form_row(form.reportedBy) }}
    <button type="submit" style="margin-top:12px; padding:8px 16px; cursor:pointer;">{{ button_label|default('Save') }}</button>
    <a href="{{ path('admin_driver_history_entry_index') }}" style="margin-left:8px;">Cancel</a>
{{ form_end(form) }}
