{{ form_start(form) }}
    {{ form_row(form.code) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
    <a href="{{ path('admin_region_index') }}" class="btn btn-link">Cancel</a>
{{ form_end(form) }}
