{{ form_start(form) }}
    {{ form_row(form.code) }}
    {{ form_row(form.name) }}
    <button type="submit" style="margin-top:12px; padding:8px 16px; cursor:pointer;">{{ button_label|default('Save') }}</button>
    <a href="{{ path('admin_region_index') }}" style="margin-left:8px;">Cancel</a>
{{ form_end(form) }}
