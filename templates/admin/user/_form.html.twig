{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.name) }}
    {{ form_row(form.roles) }}
    {{ form_row(form.plainPassword) }}
    <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
    <a href="{{ path('admin_user_index') }}" class="btn btn-link">Cancel</a>
{{ form_end(form) }}
